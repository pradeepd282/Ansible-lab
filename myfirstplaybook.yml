--- 
- hosts: all 
  become: yes
  tasks:
  - name: install apache2 on node
    apt: 
      name: apache2
      state: present

  - name: start apache2 on nodes
    service:
      name: apache2
      state: started
